// Define an function to Print each magicions name from an array
function show_magicions(magicions) {
    magicions.forEach(function (name) { return console.log(name); });
}
// Define an array containing magicions name 
var magicions_name = ["Umair", "Adil", "Ahmad", "Hamdan"];
// Call the function to print each magicion name
show_magicions(magicions_name);
